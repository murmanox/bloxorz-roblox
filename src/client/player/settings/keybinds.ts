const keybinds = {
	left: [Enum.KeyCode.S] as Enum.KeyCode[],
	right: [Enum.KeyCode.F] as Enum.KeyCode[],
	up: [Enum.KeyCode.E] as Enum.KeyCode[],
	down: [Enum.KeyCode.D] as Enum.KeyCode[],
}

export const settings = {
	keybinds: keybinds,
}
